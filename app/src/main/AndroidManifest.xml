<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ofnicon.stopdrinking">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name="com.ofnicon.stopdrinking.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.ofnicon.stopdrinking.activities.NoActivity" />
        <activity android:name="com.ofnicon.stopdrinking.activities.YesActivity" />
        <activity android:name="com.ofnicon.stopdrinking.activities.HowItWorksActivity" />
        <activity android:name="com.ofnicon.stopdrinking.activities.NotificationActivity" />

        <receiver
            android:name="com.ofnicon.stopdrinking.core.MyAlarmReceiver"
            android:enabled="true" />
    </application>

</manifest>